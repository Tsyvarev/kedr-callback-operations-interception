module_name=@module_name@

kedr_gen_coi_templates_dir := @KEDR_COI_PREFIX_TEMPLATES@
kedr_gen_tool := @kedr_gen_tool@

kedr_coi_interceptors_dir := @KEDR_COI_PREFIX_INTERCEPTORS@

kedr_coi_core_symbols=@kedr_coi_core_symbols@

KBUILD_DIR=@KBUILD_BUILD_DIR@
PWD=`pwd`

all: ${module_name}.ko

${module_name}.ko: cfake.c read_counter.c \
	cdev_file_operations_interceptor.c file_operations_interceptor.c \
	cdev_file_operations_interceptor.h
	cat $(kedr_coi_core_symbols) > Module.symvers
	$(MAKE) -C ${KBUILD_DIR} M=${PWD} modules

file_operations_interceptor.c: $(kedr_coi_interceptors_dir)/file_operations_interceptor.c
	cp -p $^ $@

cdev_file_operations_interceptor.c cdev_file_operations_interceptor.h: cdev_file_operations_interceptor.%: cdev_file_operations.data
	$(kedr_gen_tool) $(kedr_gen_coi_templates_dir)/kedr_coi_factory_interceptor.$*/ $^ > $@


clean:
	$(MAKE) -C ${KBUILD_DIR} M=${PWD} clean
	rm -f file_operations_interceptor.c
	rm -f cdev_file_operations_interceptor.c cdev_file_operations_interceptor.h

.PHONY: all clean
